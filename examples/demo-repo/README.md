# demo consumer repo

A tiny throwaway repo used only as the fixture for `proof-gates demo`.
Every file here is planted with the exact bug classes the v0 gates
catch, so the demo prints a non-empty, ranked result.

This project will leverage typed gates to keep the prose clean. Our
review process is comprehensive and best-in-class.

It is not a linter but a typed proof gate that runs in half a second.
